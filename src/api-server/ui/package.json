{
  "name": "@hobbyproxy/ui",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && (cd dist && zip -r9 ui.zip . && mv ui.zip ../..)"
  },
  "dependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  }
}
